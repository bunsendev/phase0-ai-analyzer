# 進捗確認レポート

- 確認日: 2026-08-14
- 基準文書: `AGENTS.md`、`docs/requirements.md`、`docs/implementation-plan.md`
- 確認対象: ソースコード、テスト、ドキュメント、Git管理状態

## 1. 現在の進捗率

全体の進捗率は**約13%**です。

Day 1の実装部分はおおむね完了していますが、Day 2以降の業務機能は未着手です。現在の変更の多くが未コミットである点は、進捗率とは別の管理上の注意事項です。

## 2. 完了している項目

実装と動作確認ができているものは以下です。

- Pythonプロジェクト構成と依存関係定義
- `.env`および環境変数の読み込み基盤
- `AI_PROVIDER=mock`の設定読み込み
- SQLite初期化
- 要件にある保存先7種類の初期テーブル
- Streamlit起動画面
- ファイル配置だけでは分析しない初期状態
- READMEのセットアップ・起動・テスト手順
- `.env`、DB、業務ファイルのGit除外設定
- pytestによる設定、DB初期化、画面起動テスト
- StreamlitサーバーのHTTP 200応答

主な成果物:

- `app.py`
- `src/phase0_analyzer/config.py`
- `src/phase0_analyzer/database.py`
- `src/phase0_analyzer/ui/home.py`

## 3. 作業中・一部完成の項目

### SQLite永続化

- 必要なテーブルは作成済み
- 登録、検索、履歴追加、確定処理は未実装
- 過去結果を上書きしないことは、実際の保存処理ではまだ検証できない

### AI Provider

- `AI_PROVIDER=mock`の設定読み込みとアプリシェルの起動は確認済み
- `MockAIProvider`そのものと分析フローは未実装

### ステータス

- `files.status`の初期値`READY`は存在
- 全8ステータスの定義、遷移、検証は未実装

### 環境変数

- `.env.example`には将来用の設定値も定義済み
- 現在の`Settings`が利用するのは基本ディレクトリ、DB、AIプロバイダー、ログレベルなどに限定される

### Day 1管理作業

- 実装成果物は存在する
- ローカル業務サンプルは`data/upload/`に存在しない
- 実装ファイルの多くがまだGitにコミットされていない

## 4. 未着手の項目

### Day 2

- アップロードフォルダ走査
- ファイルDB登録
- 重複防止
- ファイル一覧画面
- 一覧更新
- 分析対象選択
- 分析開始、一括開始、再分析、対象外、詳細操作

### Day 3

- Excelパーサー
- CSV文字コード判定とパーサー
- PDFテキスト抽出とOCR切り替え
- 画像メタデータ・OCR処理
- 共通パーサーインターフェース
- 対応外・破損ファイル処理

### Day 4

- Mock AI Provider
- OpenAI Provider
- OCR Provider
- AI JSONスキーマ
- Pydantic検証
- 5分類
- 帳票種類、提供元、対象日、項目、要約、警告の抽出
- 過去確定結果の上位3件再利用

### Day 5

- 分析結果画面
- 元データプレビュー
- 要確認判定
- 修正保存
- 結果確定
- 修正履歴
- 再分析時の過去結果保持

### Day 6〜7

- 実データ検証
- 手書き・低品質画像検証
- 複雑PDF検証
- 不具合修正
- 利用者フロー確認
- 最終文書整理と振り返り

## 5. テスト状況

実行結果:

```text
5 passed in 1.75s
```

- pytest件数: **5件**
- 成功: **5件**
- 失敗: **0件**

確認済み:

- 環境変数読み込み
- SQLite URLのエラー
- SQLite初期スキーマ
- DB初期化の冪等性
- `AI_PROVIDER=mock`でのStreamlit画面実行
- 起動だけでは分析履歴が作成されないこと

未テストの主要機能:

- ファイル走査、登録、重複防止
- 一覧画面とボタン操作
- 全ファイル形式の解析
- OCR、AI処理
- 5分類
- 要確認判定
- 修正、確定、再分析
- 履歴の追記保存
- 過去事例の再利用
- 日本語ユーザーエラー全般

## 6. アプリ起動状況

**現在の`.venv`を使えば起動可能**です。

確認結果:

- Python: 3.12.13
- Streamlit: 1.61.1
- HTTP応答: 200
- pytest上の画面実行: 成功

ただし、READMEの新規セットアップ手順を現在のシェルでそのまま実行すると、システムの`python`コマンドがPATH上で利用できず終了コード9009になります。既存の`.venv`内のPythonは正常です。

- 既存環境からのアプリ起動: **可能**
- READMEを使った完全な新規構築: **このPCではPythonのPATH設定が必要**

## 7. 仕様との差分

### 仕様にあるが未実装

要件の業務機能は、ほぼDay 2以降として未実装です。受入条件のうち現在満たしているのは、起動、pytest実行、配置だけでは分析しないことの確認に限られます。

- 共通フォルダ走査
- ファイル一覧
- 手動分析開始
- Excel、CSV、PDF、画像処理
- OCR、AI
- 5分類
- 結果表示
- 修正、確定
- 修正履歴
- 再分析時の過去結果保持
- mock AIによる一連動作

### 仕様に明記されていないが実装されているもの

- `schema_metadata`テーブル
- 起動画面での環境、AIプロバイダー、アップロード先表示
- パッケージバージョン

いずれも初期構成やDB管理の範囲であり、プロトタイプの対象外機能には該当しません。メール取得、認証、外部連携、自動分析などの先行実装はありません。

## 8. 既知の問題・リスク

### OneDriveとSQLite

DBの既定位置はOneDrive配下の`data/database/phase0.db`です。

- OneDrive同期中のDBファイル競合リスクがある
- 現在はSQLite既定のジャーナル方式で、競合対策やタイムアウト設定はない
- 複数プロセス、複数利用者利用は未対応
- フェーズ0の単一利用者運用では直ちに致命的ではないが、同期競合時は非同期フォルダへの`DATABASE_URL`変更が回避策になる

### `.env`とAPIキー

- `.env`は存在しない
- `.env`は`.gitignore`対象
- `.env`はGit管理対象ではない
- `.env.example`の`OPENAI_API_KEY`は空
- APIキー形式の文字列は検出されなかった

### 業務ファイル

- `data/upload/`でGit管理されているのは`.gitkeep`だけ
- `data/upload/*`はGit除外対象
- 現在、業務ファイルは配置されていない

### 履歴の上書き

- 分析、修正の更新処理自体がまだ存在しない
- テーブルは履歴行を追加できる構成だが、上書きを禁止する処理やテストは未実装
- 現時点では「上書きされない」と最終保証する段階ではない

### `AI_PROVIDER=mock`

- 設定読み込みと起動画面は正常
- Mock AI分析処理は未実装なので、受入条件の「mock AIで一連動作」は未達

### 自動分析

- ファイル監視、ファイル走査、分析オーケストレーターは存在しない
- 起動テストでは分析ボタンがなく、分析履歴も0件
- ファイル配置だけで自動分析される実装はない

### Git状態

確認時点で以下が未コミットです。

- `.gitignore`、README、既知課題の変更
- アプリ、ソース、テスト、`pyproject.toml`などの新規ファイル

現在の成果を確実に保存、共有するには、次の作業前に差分確認とコミットが推奨されます。

## 9. 次に行うべき作業

### 1. Day 1成果物の確定

- 作業内容: 差分確認、秘密情報確認、コミット、READMEのPython前提補足
- 完了条件: クリーンなGit状態、または意図したコミットが作成済み
- 依存する作業: なし

### 2. ファイル検出・DB登録

- 作業内容: `data/upload/`の手動走査、メタデータ登録、重複防止
- 完了条件: 一覧更新操作で新規ファイルが一度だけ登録される
- 依存する作業: SQLite初期構成

### 3. ファイル一覧画面

- 作業内容: 要件の一覧列、選択、更新、対象外、詳細導線
- 完了条件: 登録済みファイルとステータスを表示できる
- 依存する作業: ファイル検出、DB登録

### 4. 手動分析開始の境界

- 作業内容: 分析開始ボタンとオーケストレーターの空実装、履歴追加
- 完了条件: 配置や一覧更新では分析せず、ボタン押下時だけ実行履歴を追加する
- 依存する作業: ファイル一覧、DB登録

### 5. 共通パーサーと形式別解析

- 作業内容: Excel、CSV、PDF、画像の共通インターフェースと解析
- 完了条件: 必須6拡張子を判定し、仕様所定のメタデータを返す
- 依存する作業: 手動分析開始の境界

## 10. 今日の到達状況

**Day 1レベルまで到達し、Day 2は未着手**と判定します。

Day 1の実装成果物とテストは揃っています。ただし、ローカルサンプル準備、未コミット差分の確定、READMEを使った新規環境構築の確認には残作業があります。

## 最終判定

**今すぐ次の実装へ進んでよい**

現行コードのテストが全件成功し、アプリ起動も確認でき、Day 2を妨げる実装上の障害がないためです。ただし、次の実装前に現在の差分を確認してコミットし、Day 1の復元地点を作ることを強く推奨します。
