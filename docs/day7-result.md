# Day 7 完了報告

作成日: 2026-08-14

## 1. 変更ファイル

### 実装・設定

- `.env.example`: OCR要確認とAI入力上限の設定例を追加
- `src/phase0_analyzer/config.py`: 新しい設定値を追加
- `src/phase0_analyzer/ai_input.py`: AI入力を安全な範囲へ整形する処理を追加
- `src/phase0_analyzer/analysis_service.py`: OCR要確認判定とAI入力制限を分析フローへ組み込み
- `src/phase0_analyzer/review.py`: `OCR_REVIEW_REQUIRED`を要確認理由として扱う
- `src/phase0_analyzer/result_service.py`: ファイルごとの最新分析情報取得を追加
- `src/phase0_analyzer/ui/home.py`: 一覧項目と横長Excel表示を改善

### テスト

- `tests/test_ai_input.py`
- `tests/test_analysis_service.py`
- `tests/test_app_startup.py`
- `tests/test_config.py`
- `tests/test_result_service.py`
- `tests/test_review.py`

### ドキュメント

- `README.md`
- `docs/requirements.md`
- `docs/decisions.md`
- `docs/known-issues.md`
- `docs/test-plan.md`
- `docs/document-map.md`
- `docs/operator-validation-guide.md`
- `docs/phase0-completion.md`
- `docs/next-phase-plan.md`
- `docs/day7-result.md`

Day 6の検証記録である`docs/day6-result.md`には、Day 7開始前の進捗スナップショットが追記されている。

## 2. JPEG要確認ルール

- 画像、画像PDFなどParserが`requires_ocr=true`としたファイルへ、既定で`OCR_REVIEW_REQUIRED`警告を追加する。
- Mock OCRを使用する現在のJPEG分析は`REVIEW_REQUIRED`になる。
- 表示文言は「画像・手書き帳票のため、OCR結果を確認してください。」とした。
- `OCR_REVIEW_REQUIRED=false`で解除できる。実OCRの精度確認後に運用変更でき、画像形式そのものを永久に要確認とするハードコードではない。
- 実データJPEGでRun 5を作成し、`REVIEW_REQUIRED`、警告、Mock OCR結果、元画像の表示を確認した。

## 3. AI入力制限

- 既定値を`AI_MAX_COLUMNS=100`、`AI_MAX_INPUT_CHARS=100000`とした。
- AIへ渡す段階だけで、値のある列を優先し、元の列順を維持して選択する。
- 列数または文字数の上限を超えた場合は切り詰め、`AI_INPUT_TRUNCATED`警告を分析結果へ保存する。
- snapshot、Parser結果、DBへ保存する抽出全文は切り詰めない。

実データの102行×1221列XLSXでは、Parser previewは1221列を保持したまま、AI入力を100列、61,314文字、30 preview行へ制限した。100,000文字の上限内であり、全1221列をAI入力へ渡していない。

## 4. 横長Excel表示改善

- 詳細画面では値のある代表列を最大設定列数まで表形式で表示する。
- 「全1221列中、現在100列を表示しています。」と表示列数を明示する。
- 全列のJSON表示は「全列を表示」トグルの内側へ移した。
- 実データXLSXで代表100列、制限警告、折りたたみ表示を確認し、表変換例外がないことを確認した。

高度な業務列選択、列検索、列見出しの業務名変換は次フェーズ候補である。

## 5. 一覧画面改善

一覧へ次を追加・確認した。

- ファイル名
- ファイル形式
- ステータス
- 最新AI分類
- 最新帳票種類
- 要確認件数
- 最新run
- 詳細表示の有無

最新情報は各ファイルの最新の非失敗runから取得する。Streamlit AppTestで9行、上記列、例外0件を確認した。

一括分析開始と`EXCLUDED`操作は、担当者確認前に操作を増やす必要性が確定していないため、フェーズ0対象外・次フェーズ候補とした。

## 6. 過去事例再利用の判断

選択はBである。フェーズ0は確定結果と修正履歴の保存までとし、上位3件の検索と`AnalysisRequest.previous_examples`への投入は次フェーズへ移管した。

単純一致の妥当性、誤った確定結果による誘導、どの特徴を一致条件とするかを実データで評価できていないため、Day 7での最小実装でも過剰実装になると判断した。決定は`docs/decisions.md`のD-020へ記録した。

## 7. TXTの正式方針

- フェーズ0ではTXT Parserを追加しない。
- CP932・CSV構造のTXTは、元ファイルを変更せずCP932を維持した`.csv`コピーを作り、既存CSV Parserで検証する。
- 正式TXT対応の要否は次フェーズで判断する。

## 8. 担当者確認手順

`docs/operator-validation-guide.md`を作成した。10〜15分で、検証ファイル選択、一覧更新、分析開始、要確認、元データ比較、修正、確定まで確認できる。迷った箇所、不要操作、比較しやすさ、負担増減などの回答欄も用意した。

## 9. フェーズ0完了判定

`docs/phase0-completion.md`へ21項目を、完了、一部完了、未完了、次フェーズへ移管で整理した。

実装面では、手動登録、snapshot、Parser、Mock OCR/AI境界、明示的な分析開始、詳細、要確認、修正、確定、再分析、履歴保持まで成立している。担当者操作確認、OneDrive競合、外部送信・セキュリティ判断が残るため、総合判定は「一部完了（担当者操作確認待ち）」とした。

## 10. 次フェーズ候補

`docs/next-phase-plan.md`へ次の3分類で整理した。

- A. 早期対応: 実OCR評価、実AI接続準備、業務列選択、担当者UI改善、過去事例再利用の評価
- B. 実運用前に必要: 個人情報・機密情報ルール、外部送信承認、APIキー・コスト管理、OneDrive/SQLite運用、監査・同時利用方針
- C. 将来対応: TXT正式対応、タブレット入力、メール自動取得、自動分析、外部連携、モデル再学習

## 11. テスト件数・結果

実装後の全pytest結果は次のとおり。

- 件数: 56件
- 成功: 56件
- 失敗: 0件

確認対象には、設定によるJPEG要確認、設定解除、列数上限、文字数上限、切り詰め警告、Parser元結果保持、一覧最新情報を含む。

最終確認コマンドの結果:

- `.\\.venv\\Scripts\\python.exe -m pytest`: 56 passed
- `.\\.venv\\Scripts\\python.exe -m pip check`: `No broken requirements found.`
- `git diff --check`: 問題なし

現在の非アクティブなPowerShellでは`pytest`単体がPATH上にないため、リポジトリの仮想環境を明示して同じpytestを実行した。

Streamlitでは次を確認した。

- JPEG: 分析、`REVIEW_REQUIRED`、警告、Mock OCR結果、元画像表示
- 横長XLSX: 分析、100列・61,314文字への入力制限、代表列表示、修正履歴保存、確定

## 12. 残課題

- 業務担当者本人による操作確認と回答回収
- 実OCR・実AIの精度評価と外部送信承認
- 個人情報・機密情報、APIキー、コスト上限の運用決定
- OneDrive同期中のSQLite、snapshot、ファイル競合試験
- 横長Excelから業務上重要な列を選ぶ方法
- CSV・PDFを含む実業務形式の追加検証
- OCR警告の確認済み状態、画像の回転・ぼけ・暗さ判定
- 過去事例再利用の検索条件と誤誘導対策

## 13. フェーズ0を完了扱いにできるか

**開発成果は担当者確認に渡せる状態だが、フェーズ0の正式完了扱いにはまだしない。**

中核フロー、履歴保持、重大な入力安全策は完成し、既知のデータ破損・履歴消失はない。一方、フェーズ0の目的に含まれる担当者操作確認が未実施であり、OneDrive競合と外部送信・セキュリティ判断も未確定である。担当者確認結果を記録し、重大不具合がないことを確認した時点で、プロトタイプ開発完了として承認するのが適切である。

## 14. Streamlit UI仕上げ

### 簡略化した内容

- 画面全体を「① ファイル登録、② 分析開始、③ 結果確認」の3ステップへ統一した。
- 分析対象と結果表示対象の2つの選択欄を、1つの「分析するファイル」へ統一した。
- 一覧をファイル名、形式、状態、最新分析結果、要確認、次に行う操作へ絞った。
- 内部ステータスと分類コードを日本語表示へ変更した。
- 分析済み、要確認、確認済みの各状態で、次に行う内容を案内する文言を追加した。

### 非表示・折りたたみにした内容

- 実行環境、AIプロバイダー、アップロード先をトップ画面から除外した。
- run番号、処理方式、モデル名、confidence、判定理由、警告一覧、Parser metadata、raw AI response、確定者情報を「詳細情報（開発・調査用）」へ移した。
- 横長表の全列表示を「元データの全列を表示」へ折りたたんだ。

### 担当者向けに変更した文言

- `READY`等の内部ステータスを「未分析」「分析中」「分析済み」「要確認」「確認済み」「エラー」へ変更した。
- `ORDER`等の分類コードを「受注・依頼」「在庫」「出荷・配送」「その他」「判定不能」へ変更した。
- 「一覧更新」を「ファイル一覧を更新」、「分析対象」を「分析するファイル」、「分析run」を「表示する分析履歴」へ変更した。
- Mock固有の提供元と要約は通常画面で「未判定」と自然文へ置き換え、元の値は詳細情報に保持した。

### ブラウザ確認

- 未分析: 「未分析確認.csv（未分析）」に対して主ボタン「分析開始」と、③の「②の分析開始を押してください」を確認した。
- 分析済み: 「メディア (13).jpg（分析済み）」に対して「③ 結果確認」で同じファイルの結果、要確認なし、元画像、修正・確定操作を確認した。
- 要確認: 「メディア (10).jpg（要確認）」に対して、選択直後の注意文と「画像・手書き帳票のため、OCR結果を確認してください。」が最初に確認できることを確認した。

3ケースとも、選択したファイルと結果表示が一致し、次の主操作を日本語で判断できる。再分析、過去分析履歴、修正、確定の既存操作も維持している。

### 残るUI課題

- 業務担当者本人による操作確認は未実施。
- 狭い画面では一覧の横スクロールが必要。
- 抽出項目が多い帳票では修正欄が縦に長くなる。
- 分析済みファイルの再分析ボタンをどこまで目立たせるかは担当者確認が必要。

## 15. フェーズ0検証版の完成作業

更新日: 2026-08-16

### 変更内容

- 未分析、分析中、分析済み、要確認、確認済み、エラーごとの案内文と次操作を整理した。
- 再分析を「必要な場合だけ再分析」へ折りたたみ、結果確認を優先した。
- 要確認件数を、重複しないwarning/errorコード、要確認field、分類、帳票種類から算出するようにした。
- 結果画面を「何のデータか、要確認件数、主な内容、元データ、修正・確認」の順にした。
- 詳細情報へ分析開始日時、分析完了日時、`duration_ms`の秒表示を追加した。
- `docs/operator-validation-result.md`を追加し、担当者評価と操作時間を記録できるようにした。
- `docs/phase0-completion.md`をミニマム開発の完了条件へ更新した。
- `docs/development-learning-review.md`へ工程、成果物、実問題、学びを整理した。

### 要確認件数

- 同じ警告コードが複数保存されていても1件とする。
- warning/error、要確認field、分類低信頼または判定不能、帳票種類低信頼を別理由として数える。
- 結果全体の要確認フラグしか理由がない場合は最低1件とする。
- 一覧と結果詳細は同じ`ResultService`の算出結果を使用する。

### 最終実行確認の状態

- 追加後の想定pytest件数: 57件
- `git diff --check`: 成功（CRLF変換予告のみ）
- `pytest`: Windowsアプリケーション制御が`.venv\\Scripts\\python.exe`を起動拒否したため未完了
- `pip check`: 同じ起動拒否により未完了
- Streamlit 4ケース: 同じ起動拒否により今回の変更後確認は未完了

Day 7直前の最終確認では56件が成功している。今回の実装を正式な検証版とする前に、実行制御を解消して57件と4ケースを再確認する。

### 現在の完了判定

コードと担当者向け資料は、担当者検証へ渡す内容まで実装済みである。ただし、最新変更後の自動テストとStreamlit確認が環境要因で未完了のため、現時点では「検証版実装完了・最終実行確認待ち」とする。実行確認成功後、担当者が`docs/operator-validation-result.md`を記入し、重大不具合がなければフェーズ0を完了できる。

## 16. UIアクセス環境の整理

2026-08-16にPython、仮想環境、PowerShell実行ポリシー、Code Integrityログを確認した。

- 現在の`.venv`はCodex同梱Python 3.12.13由来。
- `.venv/Scripts/python.exe`は未署名扱いで、WDAC Policy ID `{0283ac0f-fff1-49ae-ada1-8a933130cad6}`に拒否された。
- バンドルPythonも`libssl-3-x64.dll`が同じ署名要件で拒否された。
- Code Integrityイベントは3033・3077。
- PowerShell実行ポリシーは`LocalMachine=RemoteSigned`であり、今回の原因ではない。
- 端末には別の利用可能なPythonがインストールされていない。
- `.env`は現在存在せず、アプリは既定のOneDrive配下DB・snapshotを参照する構成。

`docs/local-ui-test-guide.md`を追加し、次を整理した。

- 管理者へ伝えるWDAC許可依頼情報
- 承認済みPython確認と仮想環境再構築
- venv、SQLite、snapshot、workを`%LOCALAPPDATA%`へ分離する推奨配置
- `.env`設定例
- Streamlit起動、`http://localhost:8501`へのアクセス、health check、停止方法
- 未分析、Excel、JPEG、修正・確定の4ケース
- pytest、pip check、git diff --checkの完了チェック

現在の`.venv`、旧DB、snapshot、実データ、`.env`は自動変更・削除していない。承認済みPythonの導入後、ガイドに沿って新しいローカル検証環境を作成する。
