# 既知課題

- Day 2時点では分析開始、解析、OCR、AI、結果・修正画面は未実装
- 一覧更新中にファイル内容が変更された場合の安定性確認は未実装
- 同じパスの変更版は履歴として複数行表示される
- 一覧の登録日時・更新日時はUTC文字列のままで、日本時間向けの表示整形は未実装
- OneDrive同期中のSQLite・アップロードファイル競合は未検証
- 登録後に同じパスの内容が変わると、原本スナップショットがない旧版は再解析できない
- Excelの行列数はopenpyxlの使用範囲に基づくため、書式だけ設定されたセルの影響を受ける可能性がある
- CSVの区切り文字は自動推定のため、不規則なファイルではカンマへフォールバックする
- PDFの表構造抽出と画像PDFへのOCR実行は未実装
- 画像の品質判定は幅・高さの閾値だけで、ぼけ・傾き・明暗は未判定
- snapshotはDBとファイルシステムをまたぐため、OS強制終了時の孤立一時ファイル回収は未実装
- run_number採番は単一利用者プロトタイプ向けで、大量同時分析は未対応
- Mock OCRは固定テキストであり、OCR精度を検証するものではない
- Mock AIは単純キーワード分類であり、実データ精度を検証するものではない
- `ai_results`旧テーブルは互換性のため残しており、Day 4では`analysis_results`を使用する
- 類似帳票検索と過去事例再利用は未実装
- warningの確認済み状態は未実装
- Excel/PDF/画像プレビューは簡易表示で、ページ画像やOCR座標ハイライトは未実装
- 確定操作は追記可能で、確定取消・確定版同士の比較UIは未実装
- 修正者・確定者は`DEFAULT_USER`固定で、本番認証とは連携していない
- 日時と信頼度の表示整形は最小限
- Day 6実データ9件はParser確認まで実施済みだが、正解情報を用いた精度評価と担当者負担評価は未実施
- 提供された受注TXT 4件は現仕様の対応形式外であり、そのままでは分析できない
- 横長ExcelのAI入力は100列・100,000文字へ制限済みだが、業務的に重要な代表列を選ぶ機能は未実装
- JPEG・画像PDFは設定により要確認化したが、回転・手書き・小さい文字・ぼけ・暗さを個別判定する機能は未実装
- 横長XLSXは代表100列を表表示できるが、列見出しの業務名表示と列選択UIは未実装
- 個人情報・機密情報・外部AI送信可否・想定件数・コスト条件が未確認
- validationファイルは通常一覧と同じ`files`テーブルに表示され、検証データ専用フィルターは未実装
- SQLiteスキーマのマイグレーション機構は未実装（初期スキーマのみ）
- Python 3.11以上のローカル実行環境と依存パッケージの導入が必要
- 手書きOCR精度は実データ検証が必要
- AI信頼度と実正解率は一致しない可能性
- 複雑PDF表は構造化失敗の可能性
- 複数ページ表結合は対象外
- 帳票改版検知は簡易版
- 大量ファイル未検証
- 複数利用者同時操作は対象外
- 本番認証・権限管理は未実装
- 業務担当者本人による10〜15分の操作確認は未実施
- 一覧はStreamlit標準dataframeのため、画面幅が狭い場合は横スクロールが必要
- 修正項目が多い帳票では入力欄が縦に長くなり、対象項目の絞り込みは未実装
- 2026-08-16の確認環境ではWDAC Policy ID `{0283ac0f-fff1-49ae-ada1-8a933130cad6}`により、未署名の`.venv/Scripts/python.exe`とCodex同梱Pythonの`libssl-3-x64.dll`がCode Integrityイベント3033・3077で拒否される。承認済みPythonの導入手順は`docs/local-ui-test-guide.md`に記載。最新変更後のpytest・pip check・Streamlit確認は未実施で、Day 7直前の最終結果は56件成功

各課題には、発生日・影響・回避策・今回対応/持越しを追記する。
